import Colors from 'themes/Colors';

export default ({
  container: {
    flex: 1,
    backgroundColor: Colors.whiteGray
  },
  absolute: {
    position: "absolute",
    top: 0, left: 0, bottom: 0, right: 0,
  },
});
